//src/app/pridat/page.tsx

import Typography from '@mui/material/Typography';

export const metadata = {title: "Pridať post | zoska snap"}

export default function AddPost() {
  return (

      <Typography > O mne </Typography>

  );
}